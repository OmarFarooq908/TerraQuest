[project]
name = "adventure-gis"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "adventure-core",
  "duckdb>=1.1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
adventure-core = { workspace = true }
