# Examples

Copy-paste mission prompts and small scripts for local exploration.

See `mission_skardu_rules.sh` after Phase 3 docs land; until then:

```bash
uv run adventurectl mission run \
  --pack fixtures/karakoram_mini \
  --interpreter rules \
  -p "Three days, Suzuki Swift, rivers and forests, hate crowds."
```
