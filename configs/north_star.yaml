# North Star measurement pin (RFC-0005 / issue #29)
# Loaded by adventure_core.evaluation + scripts/eval_discovery.py defaults.
# Keep in sync with adventure_core.evaluation.NORTH_STAR_* constants.
# Default prompt text lives in scripts/eval_discovery.py::DEFAULT_PROMPT.

primary_metric: recall_at_k
primary_label_filter: interesting
k: 5
guardrail_metric: popularity_trap_at_k
popularity_threshold: 7.0
match_radius_km: 2.0
default_mode: fearless_far
freeze_note: >
  Primary metric frozen for ~1–2 months from RFC-0005 merge.
  Fixture recall is smoke only; real claims need regional labels.
